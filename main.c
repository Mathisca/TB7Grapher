#include "main.h"

int main() {
    // testInterpreter();

    //srand(time(NULL));

    //PARTIE CODE INTERPRETER (LOUISE ET FLAVIE)
   /* double res = test_evaluation();
    printf("f(x) = %lf", res);
   */

    testAnalyst();
    return 0;
}